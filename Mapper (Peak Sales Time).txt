#!/usr/bin/env python
import sys

for line in sys.stdin:
    line = line.strip()
    data = line.split(',')

    # Skip header
    if data[0] != "order_id":
        time = data[5]
        hour = time.split(':')[0]
        print(hour + "\t1")
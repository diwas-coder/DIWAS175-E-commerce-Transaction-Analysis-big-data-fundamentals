#!/usr/bin/env python
import sys

for line in sys.stdin:
    line = line.strip()
    data = line.split(',')

    # Skip header
    if data[0] != "order_id":
        product = data[1]
        quantity = data[3]
        print(product + "\t" + quantity)